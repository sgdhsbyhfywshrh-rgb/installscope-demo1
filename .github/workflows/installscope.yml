name: installscope
on:
  pull_request:
    paths: ["**/package-lock.json", "**/pnpm-lock.yaml"]
permissions:
  contents: read
jobs:
  record:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Pinned to a full commit of main (2026-09-20), the safest way to pin an action.
      - uses: mukti-sys/InstallScope/action/record@d14c31562b75676d0448fb7092f7a8074bd4f263
